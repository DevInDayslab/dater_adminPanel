# Local dev with Vite proxy (recommended)
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://127.0.0.1:3000

# Production (Vercel): defaults to https://api.dater.social/api/v1 — no env needed.
# Override only if using a different API host:
# VITE_API_BASE_URL=https://api.dater.social/api/v1
