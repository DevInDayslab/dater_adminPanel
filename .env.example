# Local dev: relative path goes through Vite proxy (see vite.config.ts).
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://127.0.0.1:3000

# Production builds use .env.production (https://api.dater.social/api/v1).
