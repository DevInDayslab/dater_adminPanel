# Local dev with Vite proxy (recommended)
VITE_API_BASE_URL=/api/v1
VITE_API_PROXY_TARGET=http://13.201.72.214:3000

# Vercel production: use same-origin proxy (see vercel.json) — avoids HTTPS→HTTP mixed-content blocks
# VITE_API_BASE_URL=/api/v1

# Direct backend URL (only if backend is served over HTTPS)
# VITE_API_BASE_URL=https://your-api.example.com/api/v1
